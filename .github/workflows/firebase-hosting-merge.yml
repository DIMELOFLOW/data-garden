# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v2
        with: 
           node-version: 18
           cache: 'npm'
      - name: Install Firebase Tools
        run: npm install -g firebase-tools
      - name: Login to Firebase
        run: firebase login:ci --token "${{ secrets.FIREBASE_TOKEN }}"
      - run: npm i
      - run: npm run build
      - name: Deploy to Firebase Hosting
        run: npx firebase deploy --token "${{ secrets.FIREBASE_TOKEN }}" --non-interactive --project data-garden-f6930
     
      




# firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DATA_GARDEN_F6930 }}
